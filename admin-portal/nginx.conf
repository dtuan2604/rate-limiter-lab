server {
    listen 8080;
    server_name _;

    location = /health {
        access_log off;
        default_type application/json;
        return 200 '{"service":"admin-portal","status":"UP"}';
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

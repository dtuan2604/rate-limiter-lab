server {
    listen 8080;
    server_name _;

    location = /health {
        access_log off;
        default_type application/json;
        return 200 '{"service":"load-balancer","status":"UP"}';
    }

    location / {
        return 404;
    }
}
